import styled from "styled-components";
import {borderRadius, colors} from "../../../styles/global";

export const PieChartTabsContainer = styled.div`
  background-color: ${colors.white};
  border-radius: ${borderRadius.m};
  overflow: hidden;
  padding: 5px;
  height: 100%;
`;