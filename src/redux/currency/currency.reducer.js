const INITIAL_STATE = {
    defaultCurrency: null,
    appCurrencies: [],
};

export const currencyReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}